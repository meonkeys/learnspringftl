[_] add a small example of Spring Webflow
[_] serve "GET /hello" instead of "GET /hello.ftl"
[_] add Hibernate
	make it create & drop db on the fly
	or have another app/script to create/drop db

-----------------------------------------------------------------------
vim: ft=vo_base
